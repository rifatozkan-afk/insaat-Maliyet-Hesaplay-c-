import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.proinsaat.maliyet',
  appName: 'ProInsaat',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
